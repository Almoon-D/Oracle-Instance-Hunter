# Written into the working tree by the Hunt workflow, and by anyone testing
# locally. It is only a public key, but it is generated, not source.
ssh_key.pub

# Credentials. Nothing key-shaped should ever reach a commit, and the OCI CLI
# writes its config and key here.
.oci/
*.pem
*.key

# Scratch output from a local test run.
state/
